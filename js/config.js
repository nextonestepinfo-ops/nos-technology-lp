// config.js : 実設定（.gitignore対象。コミットしない）
// 現状はデモモード。実送信する場合は CONTACT_PROVIDER を切り替える。
export const config = {
  CONTACT_PROVIDER: "demo",
  CONTACT_ENDPOINT: "/api/contact",
  WEB3FORMS_KEY: "",
  ANALYTICS_ID: "",
};
